# Netlify 배포 기본 설정입니다.
[build]
  # 정적 배포 대상 폴더를 지정합니다.
  publish = "."

# 오디오 파일 캐시 최적화 헤더 설정입니다.
[[headers]]
  # 오디오 경로를 매칭합니다.
  for = "/audio/*"
  [headers.values]
    # 캐시 헤더를 부여합니다.
    Cache-Control = "public, max-age=31536000, immutable"
